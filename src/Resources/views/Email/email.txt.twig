{#{% for field in form.formFields %}#}

{#{% if data[field.name|sonata_slugify] is defined and field.type is not same as('Infotext') %}#}
{#{{ field.fieldLabel }}: {% if data[field.name|sonata_slugify] is iterable %}{{ data[field.name|sonata_slugify]|join(', ') }}{% else %}{{ data[field.name|sonata_slugify] }}{% endif %}#}

{#{% endif %}#}
{#{% endfor %}#}



{% for key, val in data %}{% set field = form.getField(key) %}
{% if field is defined and field.type is not same as('Infotext') %}
{{ field.fieldLabel }}: {% if val is iterable %}{{ val|join(', ') }}{% else %}{{ val }}{% endif %}
{% endfor %}