{% for field in form.formFields %}

{% if data[field.name|slugify] is defined and field.type is not same as('Infotext') %}
{{ field.fieldLabel }}: {% if data[field.name|slugify] is iterable %}{{ data[field.name|slugify]|join(', ') }}{% else %}{{ data[field.name|slugify] }}{% endif %}

{% endif %}
{% endfor %}