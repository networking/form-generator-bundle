


    <a class="btn btn-primary sonata-action-element" href="{{ path('networking_form_export', {'id': object.id}) }}" >
        <i class="glyphicon glyphicon-export"></i>
        {{ 'link_action_export'|trans({}, 'formGenerator') }}</a>
